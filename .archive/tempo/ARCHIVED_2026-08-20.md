# Tempo Archive Record

- Archived: 2026-08-20
- Review deadline: 2026-11-18
- Capability owner: `weave`
- Alternatives: Weave (retry/backoff/DLQ/idempotency state machines), Gear (CI schedule triggers), Orbit (loop scheduling)

## Reason

`tempo` was reachable from one partner only (Anvil); retry/backoff, DLQ and idempotency were already owned by Weave, Relay and Orbit. Timezone/DST and JP business-calendar guidance moved to Weave. The archived directory preserves the pre-sunset definition for the 90-day recovery window.

## Restore

1. Confirm `tempo/` does not already exist.
2. Move `.archive/tempo/` back to `tempo/`.
3. Remove this archive record from the restored directory.
4. Revert the absorbed capabilities in `weave/SKILL.md` only after verifying no newer work depends on them.
5. Restore routing (`nexus/reference/routing-matrix.md`, `agent-disambiguation.md`), pack (`_common/SKILL_PACKS.md`), profile (`~/.claude/profiles/*.json`), boundary (`_common/BOUNDARIES.md`) and catalog (`compass/reference/catalog.md`) entries.
6. Run `lint-frontmatter.py`, `lint-instructions.py`, and `validate-recipes.py`.

Do not delete this archive before the review deadline unless the restoration decision has been documented.
