# Riff Archive Record

- Archived: 2026-08-20
- Review deadline: 2026-11-18
- Capability owner: `flux`
- Alternatives: Flux (divergent reframing + facilitation), Spark (proposal output), Magi (evaluation), Void (subtraction)

## Reason

`riff` was reachable from one partner only (Magi); its four modes are owned by Flux (diverge), Spark (propose), Magi (evaluate) and Void (subtract). The residual capability was an interaction mode, not a skill. The archived directory preserves the pre-sunset definition for the 90-day recovery window.

## Restore

1. Confirm `riff/` does not already exist.
2. Move `.archive/riff/` back to `riff/`.
3. Remove this archive record from the restored directory.
4. Revert the absorbed capabilities in `flux/SKILL.md` only after verifying no newer work depends on them.
5. Restore routing (`nexus/reference/routing-matrix.md`, `agent-disambiguation.md`), pack (`_common/SKILL_PACKS.md`), profile (`~/.claude/profiles/*.json`), boundary (`_common/BOUNDARIES.md`) and catalog (`compass/reference/catalog.md`) entries.
6. Run `lint-frontmatter.py`, `lint-instructions.py`, and `validate-recipes.py`.

Do not delete this archive before the review deadline unless the restoration decision has been documented.
