# Trawl Archive Record

- Archived: 2026-08-20
- Review deadline: 2026-11-18
- Capability owner: `vector`
- Alternatives: Vector (crawl execution), Stream (extraction pipeline), Scaffold (crawler fleet infra), Cloak/Canon (collection compliance)

## Reason

`trawl` was architecture-only and delegated every execution step to other skills; routing reachability was the lowest in the roster (2 hits in `nexus/reference/`) with zero inbound COLLABORATION_PATTERNS partners. The archived directory preserves the pre-sunset definition for the 90-day recovery window.

## Restore

1. Confirm `trawl/` does not already exist.
2. Move `.archive/trawl/` back to `trawl/`.
3. Remove this archive record from the restored directory.
4. Revert the absorbed capabilities in `vector/SKILL.md` only after verifying no newer work depends on them.
5. Restore routing (`nexus/reference/routing-matrix.md`, `agent-disambiguation.md`), pack (`_common/SKILL_PACKS.md`), profile (`~/.claude/profiles/*.json`), boundary (`_common/BOUNDARIES.md`) and catalog (`compass/reference/catalog.md`) entries.
6. Run `lint-frontmatter.py`, `lint-instructions.py`, and `validate-recipes.py`.

Do not delete this archive before the review deadline unless the restoration decision has been documented.
