# Latch Archive Record

- Archived: 2026-08-19
- Review deadline: 2026-11-17
- Capability owner: `hone`
- Active definition: `hone/SKILL.md`
- Migrated references: `hone/reference/hooks/`

## Reason

`latch` was merged into `hone` so AI CLI configuration auditing and Claude Code hook design, configuration, debugging, and maintenance share one routing surface. The archived directory preserves the pre-merge definition for the 90-day recovery window.

## Restore

1. Confirm `latch/` does not already exist.
2. Move `.archive/latch/` back to `latch/`.
3. Remove this archive record from the restored directory.
4. Revert the hook capability additions in `hone/SKILL.md` and remove `hone/reference/hooks/` only after verifying no newer work depends on them.
5. Restore external routing, profile, boundary, and registry entries to `latch`.
6. Run the repository frontmatter, link, and skill validation checks.

Do not delete this archive before the review deadline unless the restoration decision has been documented.
