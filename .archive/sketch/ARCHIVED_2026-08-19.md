# Sketch Archive Record

- Archived: `2026-08-19`
- Retention deadline: `2026-11-17`
- Canonical owner: `builder`
- Reason: Gemini image-generation code, prompt, batch, style, post-processing, provenance, and content-policy capabilities were consolidated into Builder as specialized implementation recipes.

## Migrated Capability Surface

Builder now owns the active `image*` recipes and the `builder/reference/image-generation-*.md` guidance. The archived package remains a complete pre-merge copy, including its original AUTORUN schema, for reversible recovery through the deadline.

## Restore

1. Move `.archive/sketch/` back to `sketch/` and remove this archive record.
2. Revert the Sketch-derived capability, trigger, boundary, recipe, collaboration, output, and reference-map additions in `builder/SKILL.md`.
3. Remove or relocate `builder/reference/image-generation-*.md` so Builder and Sketch do not claim the same image-generation-code concern.
4. Restore external routing, boundaries, profiles, and collaboration references to Sketch where required.
5. Run frontmatter, reference-link, and repository validation checks before reactivation.

Do not permanently delete this archive before `2026-11-17` without a separate reviewed decision.
