# Mint Archive Record

- Archived: 2026-08-20
- Review deadline: 2026-11-18
- Capability owner: `radar`
- Alternatives: Radar (fixtures + factories), Cloak (PII masking), Siege (large-scale datasets)

## Reason

`mint` had zero inbound COLLABORATION_PATTERNS partners and zero peer SKILL.md mentions; fixture and factory design belongs to the test-authoring owner. The archived directory preserves the pre-sunset definition for the 90-day recovery window.

## Restore

1. Confirm `mint/` does not already exist.
2. Move `.archive/mint/` back to `mint/`.
3. Remove this archive record from the restored directory.
4. Revert the absorbed capabilities in `radar/SKILL.md` only after verifying no newer work depends on them.
5. Restore routing (`nexus/reference/routing-matrix.md`, `agent-disambiguation.md`), pack (`_common/SKILL_PACKS.md`), profile (`~/.claude/profiles/*.json`), boundary (`_common/BOUNDARIES.md`) and catalog (`compass/reference/catalog.md`) entries.
6. Run `lint-frontmatter.py`, `lint-instructions.py`, and `validate-recipes.py`.

Do not delete this archive before the review deadline unless the restoration decision has been documented.
