# Anvil Archive Record

- Archived: 2026-08-20
- Review deadline: 2026-11-18
- Capability owner: `builder`
- Alternatives: Builder (CLI/TUI implementation), Hone (personal environment + AI CLI config), Gear (packaging, completion, CI)

## Reason

`anvil` was reachable from one partner only (Hone). CLI/TUI implementation belongs to Builder, personal environment and AI CLI configuration to Hone, packaging and completion to Gear. The `hearth` capabilities anvil absorbed on 2026-08-19 move to Hone with it. The archived directory preserves the pre-sunset definition for the 90-day recovery window.

## Restore

1. Confirm `anvil/` does not already exist.
2. Move `.archive/anvil/` back to `anvil/`.
3. Remove this archive record from the restored directory.
4. Revert the absorbed capabilities in `builder/SKILL.md` only after verifying no newer work depends on them.
5. Restore routing (`nexus/reference/routing-matrix.md`, `agent-disambiguation.md`), pack (`_common/SKILL_PACKS.md`), profile (`~/.claude/profiles/*.json`), boundary (`_common/BOUNDARIES.md`) and catalog (`compass/reference/catalog.md`) entries.
6. Run `lint-frontmatter.py`, `lint-instructions.py`, and `validate-recipes.py`.

Do not delete this archive before the review deadline unless the restoration decision has been documented.
