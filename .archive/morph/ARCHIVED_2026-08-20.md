# Morph Archive Record

- Archived: 2026-08-20
- Review deadline: 2026-11-18
- Capability owner: `scribe`
- Alternatives: Scribe (document authoring + conversion), Tome (article export), Quill (README/doc content)

## Reason

`morph` carried the smallest capability set in the roster (5) against 17 reference files; format conversion is a tool invocation owned by the document skills. The archived directory preserves the pre-sunset definition for the 90-day recovery window.

## Restore

1. Confirm `morph/` does not already exist.
2. Move `.archive/morph/` back to `morph/`.
3. Remove this archive record from the restored directory.
4. Revert the absorbed capabilities in `scribe/SKILL.md` only after verifying no newer work depends on them.
5. Restore routing (`nexus/reference/routing-matrix.md`, `agent-disambiguation.md`), pack (`_common/SKILL_PACKS.md`), profile (`~/.claude/profiles/*.json`), boundary (`_common/BOUNDARIES.md`) and catalog (`compass/reference/catalog.md`) entries.
6. Run `lint-frontmatter.py`, `lint-instructions.py`, and `validate-recipes.py`.

Do not delete this archive before the review deadline unless the restoration decision has been documented.
