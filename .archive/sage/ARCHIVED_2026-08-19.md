# Sage Archive Record

- Archived: `2026-08-19`
- Retention deadline: `2026-11-17`
- Capability owner: `magi`
- Reason: YC-style founder office hours and pitch critique were consolidated into Magi's Founder Mode.
- Knowledge migration: Reusable office-hours, probing, startup-pattern, anti-pattern, action-extraction, and pitch-critique references were copied to `magi/reference/` and routed from `magi/SKILL.md`.

Do not delete this directory before the retention deadline without a separate review confirming that the migrated recipes and references preserve required behavior.

## Restore

From the repository root:

```sh
test ! -e sage
mv .archive/sage sage
rm sage/ARCHIVED_2026-08-19.md
```

Then restore Sage's external routing, boundaries, profiles, and registry entries in the same change. Audit the copied Founder Mode recipes and references under `magi/` before removing them; they may have accumulated owner-specific changes after archival.
