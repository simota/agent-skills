# Bond Archive Record

- Archived: 2026-08-20
- Review deadline: 2026-11-18
- Capability owner: `growth`
- Alternatives: Growth (re-engagement, lifecycle), Pulse (retention/cohort analysis), Cast (segment archetypes), Voice (churn signals)

## Reason

`bond` never had a differentiated Route-elsewhere block (generic BOUNDARIES stub), and its retention/re-engagement/gamification capabilities duplicate Pulse and Growth. The archived directory preserves the pre-sunset definition for the 90-day recovery window.

## Restore

1. Confirm `bond/` does not already exist.
2. Move `.archive/bond/` back to `bond/`.
3. Remove this archive record from the restored directory.
4. Revert the absorbed capabilities in `growth/SKILL.md` only after verifying no newer work depends on them.
5. Restore routing (`nexus/reference/routing-matrix.md`, `agent-disambiguation.md`), pack (`_common/SKILL_PACKS.md`), profile (`~/.claude/profiles/*.json`), boundary (`_common/BOUNDARIES.md`) and catalog (`compass/reference/catalog.md`) entries.
6. Run `lint-frontmatter.py`, `lint-instructions.py`, and `validate-recipes.py`.

Do not delete this archive before the review deadline unless the restoration decision has been documented.
