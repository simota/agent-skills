# Helm Archive Record

- Archived: 2026-08-20
- Review deadline: 2026-11-18
- Capability owner: `magi`
- Alternatives: Magi (strategic verdicts + framework lenses), Compete (market intelligence), Pulse (KPI implementation), Ledger (unit economics)

## Reason

`helm` declared in its own Route-elsewhere block that Magi decides, Compete gathers, Pulse implements and Scribe formalizes — leaving a framework-application layer between four owners. The archived directory preserves the pre-sunset definition for the 90-day recovery window.

## Restore

1. Confirm `helm/` does not already exist.
2. Move `.archive/helm/` back to `helm/`.
3. Remove this archive record from the restored directory.
4. Revert the absorbed capabilities in `magi/SKILL.md` only after verifying no newer work depends on them.
5. Restore routing (`nexus/reference/routing-matrix.md`, `agent-disambiguation.md`), pack (`_common/SKILL_PACKS.md`), profile (`~/.claude/profiles/*.json`), boundary (`_common/BOUNDARIES.md`) and catalog (`compass/reference/catalog.md`) entries.
6. Run `lint-frontmatter.py`, `lint-instructions.py`, and `validate-recipes.py`.

Do not delete this archive before the review deadline unless the restoration decision has been documented.
