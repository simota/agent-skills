# Signal Keywords → Routing Destination (full table)

**Purpose:** Natural-language keyword → routing destination mapping for CLASSIFY when no explicit subcommand is provided. Most destinations are Recipes; internal phases and invocation modes are labeled explicitly.

**Read when:** Classifying ambiguous user input that did not match a known subcommand and may need keyword-based routing.

**Disambiguation rule:** Subcommand match (first token of user input) ALWAYS wins over keyword match.

**Language rule:** Keywords are **English canonical anchors**, not a literal allowlist. Nexus absorbs language and phrasing at CLASSIFY — input in any language (Japanese, etc.) or paraphrased wording is semantically mapped to the matching Recipe by intent, not string match. Translate the user's request to its English intent first, then match. The output-language config still governs the user-facing response.

---

## Core Recipe Anchors

| Keywords | Destination |
|----------|--------|
| `bug`, `error`, `broken` | `bug` |
| screenshot/image attached + `this screen is broken`, `fix what's in this screenshot`, `see attached screenshot`, `looks wrong` (web) | `bug` — **image branch** (routing-matrix § BUG Phase Contract → IMAGE BRANCH): five-section analysis at CLASSIFY, Visual Fix Loop at VERIFY. Native mobile screen vs reference image → `MOBILE_NATIVE` `visualloop` row. **Screenshot + `improve`/`make this better` is overloaded → REDIRECT**: visual/look-and-feel → `restyle`; feature behavior → `kaizen`; usability findings only (no code) → `echo` direct |
| `feature`, `implement`, `build` | `feature` |
| `security`, `vulnerability`, `CVE` | `security` (**`mode=to-zero`** on `fix every finding the scanner reports`, `clear the security backlog`, `drive vulnerabilities to zero`, `get the SAST/SCA report to clean` — a frozen-scope sweep at a CVSS floor per `_common/FINDING_LEDGER.md`; `RISK-ACCEPTED` requires a named owner + expiry, CRITICAL/HIGH acceptance is Ask First, lapsed acceptances re-open. One named vuln → the single-pass phases) |
| `refactor`, `clean up`, `code smell` | `refactor` (single known restructure, shipped once. **Add an external review loop** — "refactor until the reviewer is clean" — → `quell profile=refactor`, which keeps refactor's behavior-preservation invariant as an Equivalence Gate) |
| `optimize`, `slow`, `performance`, `speed up`, `make it faster`, `too slow`, `latency`, `p95`/`p99`, `slow query`, `slow endpoint`, `page load is slow`, `reduce memory`, `high CPU`, `throughput`, `bottleneck`, `slow build`, `slow CI`, `bundle size`, `cold start`, `over-fetching`, `chatty API` | `optimize` (any slow layer — code/render · query · network/payload · build/bundle · infra/runtime · search; layer located before a tool is bound. **overloaded → REDIRECT**: `memory leak`/`OOM over time`/`unbounded growth` → `bug` — a leak is a defect, not a tuning target; a *steady-state* footprint reduction → `optimize`. `it got slower after deploy`/`regressed after release` → `optimize +Trail` (bisect) when correctness holds, `bug` when it doesn't. Continuous parameter self-tuning (GC/threadpool/pool/cache-size loop) → `AUTO_TUNING`. **`mode=to-zero`** on `fix every budget violation`, `get every route under its performance budget`, `clear the performance budget violations`, `all endpoints under their SLO`, `every bundle under its size budget` — a **set** of budget violations swept to zero rather than one target to one number; budgets are frozen at BASELINE, so `BUDGET-RAISED` is a blocking finding only the adjudicator may ratify. One slow endpoint with one number → the single-pass phases. Contract → `reference/optimize-recipe.md`) |
| `kaizen`, `improve`, `polish`, `enhance existing`, `incremental improvement`, `refine`, `continuous improvement` | `kaizen` (**UI-scoped → REDIRECT**: `polish`/`improve`/`enhance` a *feature* → `kaizen`; `polish the UI`/`improve the look and feel` (UI/visual/look-and-feel scoped) → `restyle` row. Contract → `reference/inline-recipes.md`) |
| `anneal`, `design audit`, `audit the design`, `brush up the design`, `brush up the codebase`, `harden the design`, `harden the architecture`, `tighten the design`, `tighten loose ends`, `firm up the design`, `design weaknesses`, `design debt sweep`, `clean up the architecture`, `design improvement`, `thoroughly brush up the design`, `fix under-specified design`, `spec-code drift cleanup`, `align spec and code` | `anneal` (**`design improvement`/`improve the design` alone is overloaded on the code-vs-visual axis → REDIRECT**: architecture/code design → `anneal`; UI/visual/look-and-feel → `restyle`. Discovery-first, behavior-preserving design-quality audit → prioritized brush-up. **`spec-code drift` splits on register-vs-fix**: a report of every divergence, no edits → `verity`; align the code and clean it up → `anneal` (**`verity`→`anneal` is the audit→fix pair**). **Disambiguation:** apply a *known* restructure to a *known* target → `refactor`; a perf number → `optimize`; improve *one feature* vs a quantified target → `kaizen`; loop control → `converge`; no code, evolution directions → `delve`; remove features → `trim`; PR-gated quality-max → `summit`/`acceptance`; author a *new* spec → `spec`; report-only conformance → `attest`/`pdm`. Contract → `reference/anneal-recipe.md`) |
| `restyle`, `redesign`, `redesign the screen`, `UI refresh`, `visual refresh`, `visual polish`, `modernize the UI`, `make it look better`, `improve the look and feel`, `design facelift`, `polish the UI`, `improve the UI design`, `update the visual design`, `restyle the page` | `restyle` (UI/visual/interaction design improvement of an *existing* surface; no target → whole-UI sweep (confirm-before-launch, top-N by friction × visibility). **Disambiguation:** code/architecture design → `anneal`; one feature vs a metric target → `kaizen`; new capability → `feature`; direction only → `vision` direct; one named fix → `palette`/`flow`/`muse` direct; full design-to-impl pipeline → `atelier` skill; LP → `funnel premium`; **drive an already-directed surface to zero findings from an *external* review engine → `burnish`** (`restyle` → `burnish` is the pair). Contract → `reference/restyle-recipe.md`) |
| `review`, `check`, `audit` (bare, overloaded — REDIRECT by object) | record coherence (docs vs code, stale info, unexplained artifacts) → `verity`; **a *change set* vs the standing decision record (ADR/spec/contract/convention)** → `abide` — the split from `verity` is the anchor, not the subject: no change anchor → `verity`, "audit *the recent changes / this PR / this release* against the docs" → `abide`; code/PR quality → `judge` direct; skill/plugin/MCP supply chain → `SUPPLY_CHAIN_AUDIT` (Chain); active malware/infection → `Chain`; design fidelity vs mockup → `DESIGN_AUDIT` (Pixel); ecosystem health → `ECOSYSTEM` (Darwin→Gauge); standards (OWASP/WCAG/OpenAPI) → `Canon`; SKILL.md normalization → `Gauge`; **a supplied prompt's residual vagueness → `PROMPT_SPEC` (Chisel `audit`)** |
| `design system docs`, `token docs`, `component catalog` | `DESIGN_SYSTEM_DOCS` (see Routing Quick Start) |
| `brainstorm`, `bounce ideas`, `riff`, `ideate`, `sounding board` | (Flux direct — single-agent) |
| `apex`, `auto-impl`, `full implementation`, `discovery to launch`, `end-to-end feature`, `ultimate` | `apex` |
| `deliver`, `build a product from scratch`, `build an MVP`, `MVP delivery`, `product lifecycle`, `ship the product`, `scope-adaptive delivery`, `build-first`, `time to first PR` | `deliver` (scope-adaptive product/MVP delivery through the minimum viable specialist chain. **Disambiguation:** one bounded capability → `feature`; high-investment fixed deep discovery→ship run → `apex`; execute an existing Charter → `enact`. Contract → `reference/deliver-recipe.md`) |
| `charter`, `instruction document`, `team charter`, `team operating manual`, `runbook for a team`, `analyze repo and design a team`, `self-driving team charter`, `team design spec`, `comprehensive repo analysis to a plan` | `charter` |
| `enact`, `run the charter`, `execute the charter`, `execute the instruction document`, `build team from charter and run`, `orchestrate the charter`, `run docs/CHARTER.md` | `enact` |
| `layer`, `operating layer`, `project operating layer`, `project skill suite`, `set up the project's agents/recipes/workflows`, `design this repo's tooling`, `repo task playbooks`, `project routing map`, `design the project's agents and recipes together`, `give this repo a skill suite` | `layer` (`Sigil[blueprint]` designs the operating-layer blueprint, Sigil authors bodies, and Nexus registers routing; `design-only` stops at the blueprint. **Disambiguation:** one delivery's team+work plan → `charter`; one skill body → `sigil`/SKILL_GEN; one feature spec → `spec`. Contract → `reference/layer-recipe.md`) |
| `goal`, `/goal setup`, `goal recipe`, `long-running goal`, `autonomous loop setup` | `goal` |
| `gedanken`, `thought experiment`, `what if`, `reason through`, `counterfactual`, `limiting case`, `reductio ad absurdum`, `intuition pump`, `steelman scenario`, `hypothetically` | `gedanken` (structured thought-experiment reasoning — hypothetical → independent lenses → controlled perturbation → adversarial refutation → a falsifier; **no code**. **Intent-anchored, not keyword-anchored**: **verdict-seeking** (wants a final go/no-go) → `magi` direct; **process-oriented** (wants the reasoning walked through, no decision demanded) → `gedanken`. Worked example: "think through whether microservices are worth it here, no code" → `gedanken` (process-oriented framing overrides the "worth it" phrasing that would otherwise suggest `magi`). Exploratory analog of `magi`; orchestrates `flux`/`magi`/`omen`/`matrix`. Trivial one-off "what if" → `flux`/`magi` direct. Contract → `reference/gedanken-recipe.md`) |
| `delve`, `deep dive`, `deep-dive a feature`, `dig into the feature`, `dig deeper into <feature>`, `unlock the feature's potential`, `evolve this feature`, `deepen this feature`, `feature evolution`, `what more can we do with <feature>` | `delve` (existing-feature deep-dive → evolution-direction dialogue — excavate a *shipped* feature to non-obvious insights → evolution directions (deepen/broaden/reframe); **no code**, stops at a named Evolution Map, hands off to `spec`/`kaizen`/`feature`/`apex`. **Disambiguation:** executes improvement vs a target → `kaizen`; shapes a *new* idea → `spec`; abstract hypothesis → `gedanken`; which-feature verdict → `essential`/`killer`/`trim`. Trivial "what could we do with X" → `flux`/`spark` direct. Contract → `reference/delve-recipe.md`) |
| `spec`, `spec out`, `spec this out`, `flesh out the spec`, `nail down the requirements`, `write the spec with me`, `talk through a feature`, `refine until the spec is locked`, `idea to spec`, `finalize the spec`, `requirements through dialogue` | `spec` (interactive feature-proposal → locked spec via deep dialogue; INTERACTIVE default, no code; hands off to `feature`/`apex`. **Disambiguation:** which-feature verdict with minimal dialogue → `essential`/`killer`; builds → `feature`/`apex`. Contract → `reference/spec-recipe.md`) |
| `cartograph`, `reverse-engineer across repos`, `reverse the codebase`, `bird's-eye view`, `bird's-eye diagram`, `overview diagram`, `fukanzu`, `architecture map`, `architecture overview`, `map the system`, `design doc from code`, `document how the system works`, `understand the system across repos`, `cross-repo architecture`, `how do these repos fit together`, `system-wide diagram` | `cartograph` (multi-repo reverse-engineering → bird's-eye architecture diagrams + design doc; **no code**. Params: `repos=a,b,c`. **Disambiguation:** one shipped feature → evolution dialogue → `delve`; one repo → team+work plan → `charter`; plan-vs-code status → `pdm`; black-box external → rebuild → `clone`; changes the system → `migrate`; domain-preset doc generation → `package`; single repo / one diagram → `lens`/`atlas`/`canvas` direct. Temporal sibling → `chronicle`. Contract → `reference/cartograph-recipe.md`) |
| `chronicle`, `repository history`, `repo history`, `commit history summary`, `summarize the commit history`, `history of the repository`, `history of the repo`, `how did we get here`, `evolution of the codebase`, `codebase evolution`, `project timeline`, `repo timeline`, `git history narrative`, `git history summary`, `narrative changelog`, `story of the repo`, `retrospective from git`, `feature history`, `bug/defect history`, `improvement history`, `decision history`, `decision log from history`, `how the decisions were made`, `ADR from git history`, `design philosophy from history`, `project ethos`, `project worldview`, `what does this project value` | `chronicle` (commit-history reverse-engineering → era timeline + storylines + decision log + per-lens deep-dives + inferred ethos; **no code**. Params: `since=<tag\|date>` · `<path>` · `repos=a,b,c` · `lenses=<set\|none>`. **Disambiguation:** one period's PR report → `launch` direct; one diff to teach → `tome` direct; which commit broke X → `trail` direct; a new forward ADR → `atlas` direct; a live judgment call → `magi` direct; one feature → `delve`; repo → team+work plan → `charter`; plan-vs-code status → `pdm`. Spatial sibling → `cartograph`. Contract → `reference/chronicle-recipe.md`) |
| `verity`, `consistency audit`, `coherence audit`, `audit the codebase and the docs`, `investigate the code and docs thoroughly`, `docs vs code`, `the docs contradict the code`, `docs and code are out of sync`, `contradictions across the repo`, `list every inconsistency`, `list the discrepancies`, `outdated documentation`, `stale docs`, `what information is out of date`, `find what no longer applies`, `dangling references in the docs`, `the README is wrong`, `why does this exist`, `nobody knows why this is here`, `no rationale anywhere`, `undocumented decision`, `unexplained constant`, `magic number with no reason`, `unknown provenance`, `orphaned code nobody can explain`, `knowledge-transfer audit`, `before the maintainer leaves`, `hand-over audit` | `verity` (codebase × documentation coherence audit → a triaged **Verity Register** of three classes — CONTRADICTION · STALE · UNEXPLAINED; two mandatory sweeps (claims→reality, reality→record), authority adjudication per contradiction (`UNDECIDED` is a first-class outcome — never auto-resolved toward the code), a mandatory **Provenance Search Record** behind every UNEXPLAINED entry, root-cause clustering, and a route per finding. **Report-only** — no code, no doc edits. **Disambiguation:** find design weaknesses *and fix them* → `anneal` (**`verity`→`anneal` is an audit→fix pair**; bare `spec-code drift` splits on register-vs-fix); what is built vs planned → `pdm` direct; conformance to *one given spec* → `attest`; external named standards → `canon` skill; design-system token denominator → `lattice`; unused files to delete → `sweep` direct; one archaeology or comprehension question → `trail`/`lens` direct; a cluster of unexplained *decisions* → `verity` → `chronicle` → `atlas`; an article's claims vs the world → `newsroom`. Spatial sibling → `cartograph`; temporal sibling → `chronicle`. Contract → `reference/verity-recipe.md`) |
| `abide`, `ADR drift`, `does this change follow the ADR`, `did we drift from the ADR`, `changes vs the ADRs`, `recent changes vs the design docs`, `audit the recent changes against the docs`, `check the diff against the decision record`, `did this PR break a decision`, `we changed it but never updated the ADR`, `undocumented architecture decision`, `no ADR was written for this`, `decision record drift`, `governance audit`, `architecture decision compliance`, `saikin no henkou to ADR no kairi`, `ADR to no kairi wo kansa` | `abide` (change-anchored governance audit → a triaged **Divergence Docket** of four classes — VIOLATES · SUPERSEDES-SILENTLY · OBSOLETE-COMPLIANCE · UNGOVERNED; the governance corpus is frozen **independently of the change set**, so records the change never touched still bind; two mandatory join directions (rules→hunks finds violations, hunks→rules finds the ADR that was owed), an **intent fork** where `UNDECIDED` is first-class and never defaults to "deliberate", security/privacy-control rules are non-supersedable, and a mandatory **Governance Search Record** behind every UNGOVERNED entry. **Report-only** — never edits a record; `SUPERSEDES-SILENTLY` routes to a *new superseding* ADR via `atlas`, never an edit of the original. **Disambiguation:** the standing record vs the code repo-wide, no change anchor → `verity` (**`verity since=` cannot see this recipe's core case** — code moved, the ADR did not, so the ADR falls outside its touched inventory); the diff's bugs/quality → `judge` direct; commit/PR packaging → `guardian` direct; conformance to *one given spec* → `attest` direct; built-vs-planned → `pdm` direct; external named standards → `canon` skill; write the ADR itself → `atlas` direct (**`abide`→`atlas` is the audit→author pair**); proof-carrying merge evidence → `acceptance`. Docket sprawling repo-wide → `abide`→`verity`; an UNGOVERNED cluster → `abide`→`chronicle`→`atlas`. Contract → `reference/abide-recipe.md`) |
| `essential`, `must-have`, `MVP definition`, `core feature`, `minimum viable`, `cut scope`, `bare minimum` | `essential` |
| `killer`, `killer feature`, `differentiator`, `WOW experience`, `decisive feature`, `competitive edge` | `killer` |
| `trim`, `remove feature`, `dead weight`, `dead-weight feature`, `cut unused feature`, `feature sunset`, `retire feature`, `delete what we don't need`, `prune features`, `kill unused feature` | `trim` (inverse of essential/killer — removal via 2-axis filter; core engine `void`. Contract → `reference/inline-recipes.md`) |
| `acceptance`, `proof-carrying PR`, `acceptance gate`, `machine-adjudicated merge`, `tier-s merge`, `payment merge`, `auth merge`, `auto-merge with evidence` | `acceptance` |
| `growth-acceptance` (alias of `acceptance layer=c`), `lifecycle gate`, `market proof`, `research proof`, `brand proof`, `insight ledger`, `incrementality gate`, `brand compiler`, `growth-brand contract`, `post-launch measurement` | `growth-acceptance` |
| `summit`, `tri-engine`, `all engines`, `claude+codex+agy`, `quality maximization`, `strategic decision`, `release-critical`, `design-critical launch` | `summit` |
| `podium`, `slide deck`, `keynote`, `conference talk`, `presentation`, `talk deck`, `speaker deck`, `onboarding kit (doc + deck)`, `learning material with companion deck`, `doc + slide`, `unified content package`, `article + slides`, `retrospective (doc + exec deck)`, `launch package (announcement + sales deck)` | `podium` |
| `newsroom`, `fact-check`, `fact check the article`, `verify sources`, `verify the sources`, `source-grounded article`, `every claim cited`, `all claims sourced`, `no speculation`, `no guesswork`, `citation audit`, `citation check`, `check for misinformation`, `check this is actually correct`, `check the article is correct`, `grounded writing`, `evidence-backed article`, `provenance for every statement` | `newsroom` (grounding-first article production/audit — Evidence Ledger → producer≠verifier citation-support audit → adversarial refutation (≥2 independent T1–T3 sources) + staleness check → remediation ≤2 cycles; confirm release-critical; ships a named **Provenance Report**, zero ungrounded facts for external-facing, unresolvable claims marked `[UNVERIFIED]` never silently dropped. **Disambiguation:** doc+slide package with polish maximization → `podium`; no grounding requirement → `tome` direct; impl-vs-spec conformance → `attest`; named-standards compliance → `canon`. Contract → `reference/newsroom-recipe.md`) |
| `wish`, `once-in-a-lifetime request`, `favor of a lifetime`, `isshou no onegai`, `begging you`, `your absolute best`, `your very best output`, `spare nothing`, `pull out all the stops`, `no second chance`, `one shot to get this right`, `masterpiece quality` | `wish` (scarcity-gated **one-shot quality-ceiling** wrapper, deliverable-agnostic; ACCEPT = all rubric dims = 3; budget-enveloped, **always confirm**. **Disambiguation:** strategic/release-critical *code* quality-max wanting engine triangulation → `summit`; iterate to a *standard* bar (revisions possible) → `converge`; bare `quality maximization` without one-shot/lifetime framing → `summit`. Casual "best quality please" on routine work → the natural recipe, wish's own Scarcity Gate recommends the downgrade. Contract → `reference/wish-recipe.md`) |
| `runway`, `design tournament`, `flagship screen design`, `best possible design`, `design quality maximization`, `compete design directions`, `product-defining screen`, `ultimate UI design` | `runway` (flagship UI design tournament — 3 orthogonal directions [brand-led / usability-led / trend-led] → persona-panel judging → winner + salvage → ceiling convergence, ACCEPT = all rubric dims = 3; named **Runway Board**; **always confirm**. **Disambiguation:** single-direction improvement to a standard bar → `restyle` (its ✓direction-gate escalates here on tied directions or a product-defining surface); acquisition/conversion LP → `marquee`; direction-only, no execution → `vision` direct; strategic *code* quality-max → `summit`. Contract → `reference/runway-recipe.md`) |
| `hallmark`, `brand identity`, `brand book`, `brand voice`, `visual identity`, `define the brand`, `brand guidelines`, `brand core`, `create a brand` | `hallmark` (brand identity package quality-max — brand-core contract-level dialogue (values/voice/prohibitions) → identity tournament (Vision ×3 → Muse tokens + Ink icons) → persona-resonance panel + differentiation refutation per `_common/ADVERSARIAL_REFUTATION.md`; named **Hallmark Charter**; confirm-before-launch. **Disambiguation:** personal/engineer branding → `compete`; propagate a *settled* brand across surfaces → `rebrand` (create→propagate pair); ship-time brand verification → `growth-acceptance`; brand equity *research* → BRAND_EQUITY (Compete). Contract → `reference/hallmark-recipe.md`) |
| `rebrand`, `brand refresh`, `apply new brand everywhere`, `new logo everywhere`, `brand migration`, `propagate the brand`, `brand consistency sweep`, `roll out the new brand` | `rebrand` (all-surface brand propagation with a proven-complete guarantee — INVENTORY freezes the touchpoint denominator → per-surface batches under brand rubric + a11y + no-regression → **RESIDUE-GATE** (old tokens/voice grep + visual scan, 2× zero) → old-brand DECOMMISSION; named **Consistency Attestation**; Ask First on big-bang / 10+ files. **Disambiguation:** no settled Brand Book yet → `hallmark` first; one surface → `restyle`; one token change → `muse` direct; technical change-completeness → `migrate`. Contract → `reference/rebrand-recipe.md`) |
| `marquee` (alias of `wish domain=lp`), `best possible landing page`, `flagship LP`, `one-shot LP`, `LP of a lifetime`, `ultimate landing page`, `premium landing page`, `landing page masterpiece` | `marquee` (wish-grade **one-shot LP production** — crystallization dialogue → 3-direction tournament [narrative-led / design-led / proof-led] → funnel premium-craft-axes production → gauntlet + ceiling convergence with **machine oracles** (Lighthouse ≥95 / CWV green / WCAG AA) + LP-specific refutation → One-Shot Gate; ACCEPT = all rubric dims = 3; **no Scarcity Gate — intentional vs `wish`** (LP demand is legitimately repeatable); named **Marquee Dossier**; **always confirm**. **Bare `landing page` is overloaded → REDIRECT**: routine LP build → `funnel premium` skill / `funnel`; wish-grade one-shot/flagship → `marquee` (this recipe); improving an *existing* LP → `kaizen`/`restyle`; in-product flagship screen → `runway`. Contract → `reference/marquee-recipe.md`) |
| `assay`, `prove the design`, `prove this is a good design`, `is this actually a good design`, `is this abstraction necessary`, `do we still need this abstraction`, `is the complexity essential`, `accidental complexity`, `prove the architecture rules hold`, `are the layering rules respected`, `architecture fitness functions`, `test the design claims`, `prove changes stay local`, `how expensive is change here`, `is our design debt real` | `assay` (experimental proof of code/architecture design claims; `apply=true` is **opt-in and never inferred**. **Disambiguation:** discover undiagnosed weaknesses and **fix** them → `anneal` (**`assay`→`anneal` is a measure→fix pair**; anneal alone when the ask is "find what's wrong"); a *known* restructure → `refactor`; merge/PR-scoped gate → `acceptance`/`summit`; blast radius without implementing → `ripple` skill; propose-only YAGNI → `void` skill (**judged vs experimented is the boundary**); coverage/flaky → `radar` skill; spec↔impl conformance → `attest`/`pdm`; UI token conformance → `lattice`. Contract → `reference/assay-recipe.md`) |
| `crucible`, `prove it's usable`, `prove the UI works`, `stress the UI`, `stress test the interface`, `hostile conditions`, `adversarial conditions`, `works on a bad network`, `works offline`, `empty state and overflow`, `edge-case content`, `long labels break the layout`, `screen reader can complete it`, `keyboard-only`, `usable one-handed`, `does it hold up in the real world`, `operability` | `crucible` (operability floor proof, binary per-cell oracle. **Disambiguation:** craft/visual ceiling of a flagship surface → `runway`; standard-bar visual improvement → `restyle`; fails under *ideal* conditions → `bug`; system load/chaos/resilience → `siege` skill; named-standard conformance only → `canon` skill; one identified a11y fix → `palette` direct; E2E suite authoring → `voyager` direct. Contract → `reference/crucible-recipe.md`) |
| `silhouette`, `looks generic`, `looks templated`, `looks like every other SaaS`, `looks AI-generated`, `indistinguishable from competitors`, `would anyone know it's ours`, `nothing distinctive about it`, `blends in with the category`, `logo-off test`, `visual differentiation`, `stop looking like everyone else`, `stop looking templated` | `silhouette` (distinction proof for a product surface. **Disambiguation:** no settled brand to derive from → `hallmark` first; craft quality maximization → `runway`; standard-bar improvement → `restyle`; positioning research with no design change → `compete` skill; novel *mechanism* rather than a look → `eureka`; operability → `crucible`. Contract → `reference/silhouette-recipe.md`) |
| `lattice`, `design system drift`, `design system is not being used`, `is the design system actually used`, `token conformance`, `hardcoded colors everywhere`, `magic numbers in the CSS`, `one-off components`, `duplicate tokens`, `inconsistent spacing across screens`, `looks like five teams built it`, `design system audit`, `design consistency sweep` | `lattice` (design-system coherence proof, steady state; precondition: a system of record exists, else `muse`/`vitrine` first. **Disambiguation:** brand *identity change* propagation → `rebrand`; design the tokens → `muse` direct; catalog components → `vitrine` direct; improve how it looks → `restyle`/`runway`; code/architecture design weaknesses → `anneal`; dead-code removal → `sweep` direct; technical change completeness → `migrate`; *between* platforms → `chorus`. Contract → `reference/lattice-recipe.md`) |
| `chorus`, `cross-platform consistency`, `cross-platform coherence`, `iOS and Android feel different`, `web and app are out of sync`, `the apps drifted apart`, `native but the same product`, `platform parity`, `same product on every platform`, `does it feel native on both` | `chorus` (cross-platform coherence proof; precondition ≥2 shipped/specced platforms. **Disambiguation:** the porting *plan* → `port` skill (`port`→`chorus` is a plan→prove pair); implement one platform → `native` skill / `MOBILE_NATIVE`; drift *within* one platform's system → `lattice` (compose: `lattice` ×N, then `chorus` across); conditions rather than platforms → `crucible`; one flagship screen's ceiling → `runway`. Contract → `reference/chorus-recipe.md`) |
| `/Nexus` (no arguments) | `proactive mode` (non-Recipe) |

---

## Specialist Skill Anchors

| Keywords | Recipe / Skill |
|----------|----------------|
| `skill audit`, `MCP supply chain`, `plugin intake`, `.claude config audit`, `Unicode Tag injection`, `curl-pipe scan`, `third-party intake` | `SUPPLY_CHAIN_AUDIT` (Chain) |
| `Shai-Hulud`, `npm worm`, `PyPI worm`, `lottie-player`, `S1ngularity`, `infected lockfile`, `C2 traffic`, `credential rotation order`, `infected` | `MALWARE_RESPONSE` (Chain → Triage → Crypt) |
| `auto-tune`, `continuous tuning`, `GC tuning`, `threadpool`, `connection pool`, `worker count`, `cache size auto-adjust` | `AUTO_TUNING` (Bolt) |
| `magi`, `what would <figure> say/do`, `how would <figure> approach this`, `channel <figure>`, `advice in the style of <name>`, `mental models of <name>`, `think like <name>`, `<name>'s framework applied to`, `expert panel on`, `conclave`, `critique this as <figure>`, `review this the way <name> would` | `FIGURE_CHANNELING` (Magi[channel/conclave/critique] — advisory reading unless `decide` is explicitly requested; every claim ATTESTED/INFERRED/SPECULATIVE). **Requires a real *named individual*** — a school/movement/collective or no person named → `flux`/`flux`; synthetic user persona → `cast`; unnamed founder-mentor advisory → `magi[advisor]` |
| `make this prompt explicit`, `specify this prompt`, `this prompt is vague`, `the prompt's instructions are ambiguous`, `turn this prompt into rules`, `prompt to spec`, `executable prompt`, `what's ambiguous in this prompt`, `remove the vague wording from this prompt`, `you are a world-class …` / persona line to dissolve, `role prompt cleanup`, `is this prompt precise enough`, `puromputo wo gutaika`, `aimai na puronputo wo meikaku ni` | `PROMPT_SPEC` (Chisel — subcommands `spec` / `scan` / `role` / `audit`). **Every anchor is prompt-scoped by construction: the object must be supplied prompt text.** A bare `the instructions are ambiguous` with no prompt attached is *not* this row — split by what the instructions live in: a repo/team operating document → `charter`; a spec or requirements doc → `scribe`/`attest`; a `SKILL.md` → `gauge`/`sigil`; the user's own live request → CLASSIFY GATE (or the overloaded-anchor one-question REDIRECT). Few-shot policy, structured output, versioning, eval gates, cost, RAG/agent architecture → `oracle` direct; a bad *output* rather than vague wording → `oracle` five-layer triage first; building a reusable prompt collection → `package domain=ai-adoption` |
| `ToS`, `Terms of Service`, `Privacy Policy`, `Tokushoho`, `Specified Commercial Transactions Act`, `terms review`, `policy gap` | `LEGAL_REVIEW` (Canon → Scribe) |
| `ICE`, `RICE`, `WSJF`, `MoSCoW`, `Kano`, `Cost of Delay`, `priority`, `prioritize`, `ranking` | `PRIORITIZE` (Rank → Magi) |
| `attribution`, `multi-touch attribution`, `MTA`, `Shapley value`, `Markov attribution`, `data-driven attribution`, `DDA`, `touchpoint credit` | `ATTRIBUTION_MODELING` (Pulse). Don't confuse with MMM / incrementality (Experiment) |
| `value proposition canvas`, `VPC`, `jobs pains gains`, `pain relievers`, `gain creators`, `problem-solution fit` | `VALUE_PROP_CANVAS` (Spark) |
| `business model canvas`, `BMC`, `lean canvas`, `business model design`, `9 building blocks` | `BUSINESS_MODEL_CANVAS` (Magi) |
| `PQL`, `product-qualified lead`, `PQA`, `product-qualified account`, `PLG conversion signal` | `PQL_MODELING` (Pulse) |
| `Fogg behavior model`, `B=MAP`, `behavior design`, `tiny habits`, `motivation ability prompt` | `BEHAVIOR_DESIGN` (Growth — `habit-formation.md`) |
| `pillar-cluster`, `topic cluster`, `content architecture`, `keyword cannibalization`, `internal linking strategy` | `CONTENT_ARCHITECTURE` (Growth) |
| `Bullseye`, `19 traction channels`, `channel selection`, `See-Think-Do-Care`, `RACE planning`, `lifecycle marketing plan` | `CHANNEL_LIFECYCLE` (Growth) |
| `CBBE`, `brand equity`, `Keller pyramid`, `brand salience`, `brand resonance`, `brand strength` | `BRAND_EQUITY` (Compete — `brand-equity.md`) |
| `pre-mortem`, `premortem`, `FMEA`, `failure modes`, `RPN`, `AP`, `failure scenario enumeration`, `what could go wrong` | `PREMORTEM` (Omen → Ripple) |
| `manual QA`, `TestRail`, `Xray`, `Zephyr`, `Qase`, `test case design`, `BVA`, `equivalence class`, `decision table`, `exploratory charter`, `manual test procedure` | `MANUAL_QA` (Matrix) |
| `test pyramid`, `trophy`, `honeycomb`, `coverage heatmap`, `flake dashboard`, `Wilson lower-bound`, `mutation overlay`, `junit.xml visualize`, `lcov visualize`, `test shape` | `TEST_INTELLIGENCE` (Canvas/Pulse) |
| `feature inventory`, `unimplemented features`, `what's built`, `what's left`, `roadmap status`, `WBS`, `work breakdown`, `delivery status`, `project status`, `is X shipped`, `plan vs code`, `docs-vs-code drift` | `PROJECT_STATUS` (PDM) — **scope**-shaped: what was planned vs what shipped. Don't confuse with PROJECT (Nexus[deliver] lifecycle), PRIORITIZE (Rank), SPEC_VERIFY (Attest), INVESTIGATE (Lens). **`docs-vs-code drift` splits on the question asked:** delivery status against a roadmap/PRD → `PDM`; a register of every contradiction / stale claim / unexplained artifact across the record → `verity` |
| `AppleScript`, `osascript`, `JXA`, `JavaScript for Automation`, `Apple Events`, `macOS automation`, `Mac desktop automation`, `app control`, `tell application`, `UI scripting`, `System Events`, `sdef`, `scripting dictionary` | `MACOS_AUTOMATION` (Hone `automate`). Note: Automator *workflow* authoring is out of scope; only the "Run AppleScript" action within Automator falls under Hone |

---

## Native App Anchors (mobile + macOS desktop)

| Keywords | Destination |
|----------|--------|
| `iOS`, `iOS implementation`, `iPhone`, `iPad`, `Swift`, `SwiftUI`, `Swift 6.2`, `Liquid Glass`, `iOS 26`, `@Observable`, `SwiftData`, `Xcode`, `App Store`, `TestFlight`, `xcrun`, `simctl`, `devicectl`, `xctrace`, `WidgetKit`, `Live Activities`, `App Intents`, `ASAuthorizationController`, `Apple Intelligence`, `Foundation Models` | `MOBILE_NATIVE` (Native) — iOS path |
| `Android`, `Android implementation`, `Kotlin`, `Jetpack Compose`, `Material 3 Expressive`, `M3 Expressive`, `Compose Multiplatform`, `Strong Skipping`, `Type-safe Navigation`, `Gradle`, `KSP`, `Android Gradle Plugin`, `AGP`, `Play Store`, `Play Console`, `adb`, `logcat`, `dumpsys`, `WorkManager`, `Credential Manager`, `Jetpack Glance`, `Gemini Nano`, `AICore` | `MOBILE_NATIVE` (Native) — Android path |
| `native app`, `native implementation`, `mobile app implementation`, `both iOS and Android`, `mobile native`, `pure native`, `Passkey mobile`, `Privacy Manifest`, `Data Safety form`, `Universal Links`, `App Links`, `App Bundle`, `staged rollout`, `phased release` | `MOBILE_NATIVE` (Native) — cross-platform / shared mobile |
| `macOS app`, `Mac app`, `SwiftUI for Mac`, `AppKit`, `NSViewRepresentable`, `NSWindow`, `MenuBarExtra`, `menu bar app`, `menu bar`, `DocumentGroup`, `NSDocument`, `document-based app`, `NavigationSplitView on Mac`, `App Sandbox`, `entitlements`, `security-scoped bookmark`, `hardened runtime`, `notarization`, `notarytool`, `Developer ID`, `Mac App Store`, `Sparkle`, `XPC`, `SMAppService`, `login item`, `launch agent`, `Mac Catalyst`, `macOS Tahoe`, `macOS 26` | `MACOS_NATIVE` (Native `macos` recipe) — building a Mac app. Automating an *existing* Mac app (AppleScript/JXA/System Events) → `Hone` `automate`; iOS/Android → `MOBILE_NATIVE` (Native) |
| `XCUITest`, `XCUIApplication`, `XCUIElement`, `XCUIElementQuery`, `accessibilityIdentifier`, `fastlane snapshot`, `Snapfile`, `SnapshotHelper`, `App Store screenshot`, `frameit`, `xcresulttool`, `xcodebuild test`, `.xctestrun`, `iOS UI test`, `swift ui test`, `status bar override`, `simctl status_bar` | `IOS_UI_TEST` (Voyager `ios`) |
| `match this design on iOS/Android`, `the screen looks wrong`, `screenshot-driven iteration`, `agent visual loop`, `simulator screenshot loop`, `Compose preview screenshot`, `RenderPreview`, `xcrun mcpbridge`, `Xcode MCP`, `XcodeBuildMCP`, `mobile-mcp`, `Android Studio Agent Mode`, `Match UI to Target Image` | `MOBILE_NATIVE` (Native) — `visualloop` Recipe; read `native/reference/agent-visual-loop.md`. Native **screen** implementation/debug against a reference. Web mockup → HTML/CSS is `Pixel`; authoring a mobile test suite is `Voyager` (`ios` for XCUITest/snapshots) |
| `Web to iOS`, `Web to Android`, `Web to native`, `port to iOS`, `port to Android`, `feature parity matrix`, `nativize`, `porting design`, `Strangler Fig mobile` | `PORTING` (Port → Native) |
| `UI terminology mapping`, `Web vs iOS vs Android UI`, `cross-platform UI naming`, `what is this component called on iOS/Android`, `HIG Material equivalent`, `compare UI parts across platforms`, `equivalent component`, `UI parity implementation` | `PORTING` (Port) — read `port/reference/ui-terminology-matrix.md` for the Web ↔ iOS (HIG) ↔ Android (Material 3) component-name matrix before per-screen specs / handoff |

---

## Loop, Migration, Reproduction & Invention Anchors

Loop-control, change-completeness, cross-language, product-reproduction/synthesis, and origination recipes (previously misfiled under Mobile Native).

| Keywords | Destination |
|----------|--------|
| `loop`, `loop engineering`, `loop-engineer this`, `make a loop`, `make this a loop`, `automate with an agent loop`, `agent loop`, `run until done`, `keep iterating until <X>`, `autonomous loop`, `set up a loop`, `ralph loop`, `loop this` | **Shape-resolve, then gate** per `_common/LOOP_PRECONDITIONS.md`: native single-session goal → `goal` · in-session rubric loop → `converge` · external-reviewer-to-zero loop → `quell` (code diff) / `burnish` (rendered UI surface) / `whet` (surviving mutants) / `newsroom` (an article's factual claims) · unattended runner (scripts/contracts/recovery) → project-local `orbit` when available, otherwise `goal` or `apex` · discovery→ship one-shot → `apex`. Five-point precondition gate (completion oracle · hard-stop bound · maker≠checker · persistent memory · drift awareness) runs at the resolved owner before launch — never ungated. Unverifiable goal or unbounded launch is refused, never downgraded |
| `quell`, `codex review until clean`, `loop until zero findings`, `until the review is clean`, `fix all review comments`, `review-fix loop`, `keep fixing until the reviewer is happy`, `drive findings to zero`, `until zero review comments`, `keep fixing until the review passes`, `refactor until the review is clean`, `review-driven refactoring`, `refactor-review loop` | `quell` (review-to-zero fix loop; **`profile=refactor`** when the diff must preserve behavior — Equivalence Gate + frozen tests + behavior-changing fixes deferred; external review engine is the oracle, Finding Ledger + dispositions make zero reachable, oscillation → FROZEN/BLOCK. **Disambiguation:** rubric bar → `converge`; merge decision with proof obligations → `acceptance`; one review, no fixing → `judge` direct; conversational per-finding fixing → `judge pair`; must survive session end → project-local `orbit` when available, otherwise `goal` or `apex`. Contract → `reference/quell-recipe.md`) |
| `burnish`, `design review until clean`, `loop until zero design findings`, `keep fixing the UI until the review is clean`, `until the design review passes`, `drive the design findings to zero`, `review-fix loop on the UI`, `keep polishing until the reviewer is happy`, `fix all the design review comments`, `until the screen matches the design file`, `loop until it matches Figma` | `burnish` (design review-to-zero loop on the **rendered** surface; **split oracle** — hard findings (a11y/contrast/overflow/token residue/perf budget) to zero, soft axes to `≥ 2`, because taste never reaches zero. Frozen Finding Charter, coordinate-free fingerprints, Appearance Gate + SPILL, identity changes always deferred. **`profile=faithful`** when the surface must match a declared reference (Figma frame / design system) — `REFERENCE-DRIFT` blocks at any severity. **Disambiguation:** the design *direction* is unsettled → `restyle` first (`restyle` → `burnish` is the pair); internal rubric evaluators, no external engine → `restyle`/`converge`; object is a code diff → `quell`; operability under degraded conditions → `crucible`; system conformance with a frozen denominator → `lattice`; one named fix → `palette`/`flow`/`muse` direct. Contract → `reference/burnish-recipe.md`) |
| `whet`, `mutation testing loop`, `kill the surviving mutants`, `drive mutation survivors to zero`, `raise the mutation score`, `are our tests actually testing anything`, `the tests pass but I do not trust them`, `high coverage weak tests`, `prove the suite catches regressions`, `test quality loop`, `sharpen the test suite` | `whet` (mutation-survivor loop; the oracle is a **mutation engine's** surviving-mutant set, the floor is a per-partition **threshold contract** rather than a single number. `EQUIVALENT` shrinks the score's denominator so it needs a failed distinguishing-test attempt and cannot be ratified by the test author; implementation-mirroring tests raise `TAUTOLOGICAL-KILL`; deleting the host code closes as `CLOSED-BY-REMOVAL`, never as a kill. **Disambiguation:** one measurement, no loop → `siege` skill; line/branch **coverage** → `radar` skill; a **flaky** suite → `radar` flaky repair (whet BLOCKs on it); code review to zero → `quell`; load/chaos/contract → `siege`. Contract → `reference/whet-recipe.md`) |
| `converge`, `iterate to rubric`, `generator-evaluator`, `evaluator loop`, `quality loop`, `iterate until accept`, `loop until it passes`, `converge to a quality bar` | `converge` (standalone, or `converge <recipe>` wrapping a generator; flatten rule for loop-recipes. Contract → `reference/converge-recipe.md`) |
| `migrate`, `migrate everything`, `change without omission`, `architecture change`, `framework migration`, `framework change`, `middleware swap`, `middleware change`, `mock to production`, `mock to prod`, `stub to real`, `change completeness`, `migrate the whole codebase` | `migrate` (case = arch \| framework \| middleware \| mock-to-prod; `case=lang` → `transmute`. Contract → `reference/migrate-recipe.md`) |
| `transmute`, `rewrite in`, `port to Rust`, `TS to Rust`, `Go to Rust`, `Python to Go`, `JS to TS`, `language rewrite`, `cross-language`, `rewrite in another language`, `idiomatic rewrite`, `differential parity` | `transmute` |
| `clone`, `replicate`, `copy this product`, `clone this product`, `faithful copy`, `complete copy`, `reproduce the product`, `rebuild like <product>`, `make a copy of <product>`, `pixel-perfect clone`, `1:1 copy`, `clone this desktop app`, `copy this desktop application`, `reproduce this app` | `clone` (faithful reproduction of an external product, parity-verified; black-box analog of `transmute`. Platform-agnostic — `target_type` web \| desktop \| mobile \| has-source \| api. `replicate` is an alias. **Bare `trace` excluded** — routes to the `Trace` session-replay skill, not here. Contract → `reference/clone-recipe.md`) |
| `fuse`, `synthesize`, `combine two products`, `combine A and B`, `merge A and B into one`, `mashup of <X> and <Y>`, `blend products`, `hybrid of X and Y`, `take A's <feature> and B's <feature>`, `best of both products`, `make one product out of two`, `fuse these apps` | `fuse` (multi-source synthesis — clone's extension. Capture **≥2** products, synthesize into **one new** product: adopt/merge/net-new per element via a **Fusion Map**, verified by a **dual oracle** + **Coherence Gate**. `sources=2..N`, mixed `target_type`. **Disambiguation:** single source / faithful copy → `clone`; net-new only inspired → `feature`/`apex`. Confirm big-bang or sources ≥ 3. Contract → `reference/fuse-recipe.md`) |
| `graft`, `mix in a concept`, `mixin <X>'s concept`, `bring <X>'s concept into our product`, `apply <X>'s paradigm to our app`, `innovate by borrowing from <X>`, `transplant a concept`, `inject <X>'s idea into our product`, `make our product innovative with <X>'s approach`, `borrow the principle behind <X>` | `graft` (concept transplant for innovation — fuse's extension, clone's fidelity-inverse. Owned **host** + **donor**; transplant the donor's **concept** (principle/mechanism, not surface) onto the host → innovation. Triple oracle: concept-fidelity ∧ host-integrity ∧ **Innovation Gate**. `host=1, donors=1..N`. **Disambiguation:** faithful surface reproduction → `clone`; peer-source surface synthesis → `fuse`; net-new feature, no donor concept → `feature`. Confirm if invasive to host core / no flag. Contract → `reference/graft-recipe.md`) |
| `eureka`, `invent`, `invention`, `invent something new`, `breakthrough`, `genuinely new`, `something nobody has done`, `nobody has done this`, `novel mechanism`, `is this new?`, `is this just the obvious next step?`, `not the obvious next step`, `not ordinary`, `prior art`, `hatsumei`, `atarashii hatsumei`; **light variant: `is this already out there?`, `check if this is new`, `novelty check`, `prior-art check`, `kore sudeni aru?`, `kishutsu ka shirabete` → `eureka depth=scout`** · **end-to-end variant: `invent and build it`, `invent and ship it`, `hatsumei shite jissou made`, `ikkitsuukan de tsukutte` → `eureka ship=true`** | `eureka` (novelty-proven invention. **Variants:** `depth=scout` = novelty-check-only rung, no `ACCEPT` by construction, mutually exclusive with `ship=true`; `ship=true` = opt-in end-to-end continuation, **never inferred** from an invention ask alone. **Disambiguation:** mechanism already proven in another domain, port it here → `graft`; combine ≥2 existing products → `fuse`; pick the strongest bet from candidates already in hand → `killer`; feature ideation inside the known solution space → `spark` skill; reason about a claim with no artifact → `gedanken`; maximize execution of a *known* deliverable → `wish`; rights/licensing/patentability → `canon[legal]`. **No nameable contradiction → it is a task, not an invention** — route out. Contract → `reference/eureka-recipe.md`) |

---

## Package / Domain Anchors

| Keywords | Destination |
|----------|--------|
| `venture`, `business plan`, `business documentation package`, `MVP dossier`, `startup dossier`, `pitch package`, `business-prep package`, `investor material bundle`, `business idea to docs`, `comprehensive product docs` | `venture` (= `package domain=startup`) |
| `package`, `document package`, `documentation package`, `generate a full package`, `project package`, `generic project docs` | `package` (auto-detect preset) |
| `research package`, `research plan package`, `literature review package`, `study design`, `methodology + analysis plan` | `package domain=research` |
| `AI adoption package`, `AI rollout plan`, `RAG plan`, `prompt library`, `AI governance package`, `internal AI enablement` | `package domain=ai-adoption` |
| `legal package`, `compliance package`, `policy pack`, `ToS + privacy + AI policy drafts`, `legal risk register` | `package domain=legal` |
| `SaaS package`, `SaaS productization docs`, `AI product platform docs` | `package domain=saas` |
| `media package`, `content operations kit`, `editorial calendar package`, `channel + monetization plan` | `package domain=media` |
| `growth package`, `growth experiment plan`, `funnel + A/B plan package`, `growth hypotheses package` | `package domain=growth` (planning-only; execution → `kaizen`) |
| `pack`, `skill pack`, `skill profile`, `enable skills`, `switch profile`, `skill preset` | `pack` (skill-profile switch — confirms diff before writing `settings.json`) |

---

## Fallback

| Keywords | Destination |
|----------|--------|
| unclear or multi-domain request | internal `CLASSIFY` phase → `reference/intent-clarification.md` |
